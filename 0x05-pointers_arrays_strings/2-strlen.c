#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - gets the length of the string
 *
 * @s: the string
 *
 * Return: the length of @s
 */
int  _strlen(char *s)
{
	return (strlen(s));
}
