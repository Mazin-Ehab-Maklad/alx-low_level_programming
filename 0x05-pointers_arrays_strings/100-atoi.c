#include "main.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/**
 * _atoi - convert a number to integer
 *
 * @s: the string to be converted
 *
 * Return: an integer
 */
int _atoi(char *s)
{
	int i;

	i = atoi(s);
	return (i);
}
